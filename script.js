// 1
let x = 100;
if (x < 50) {
  console.log(`x less than 50 `);
} else if (x > 20) {
  console.log(`x much more than 20 `);
} else {
  console.log(`error`);
}
// 2
let z = `mariami`;
if (z == `mariami`) {
  console.log(`TRUE`);
} else {
  console.log(`FALSE`);
}
// 3
let userName = `mariami`;
switch (userName) {
  case `mariami`:
    console.log(`True`);
    break;
  default:
    console.log(`False`);
}
